# CoolCurve — Mac mini M5 Pro 전용 시험판

CPU 18개 코어와 GPU 7개 센서를 모두 읽고, 그중 가장 높은 온도로 팬 한 개를 조절합니다. 현재 확인한 Mac17,16 / macOS 27만 대상으로 합니다.

| 온도 | 목표 RPM |
|---|---:|
| 50°C 이하 | 1000 |
| 55°C | 1200 |
| 60°C | 1600 |
| 65°C | 2000 |
| 70°C | 2400 |
| 75°C | 2800 |
| 80°C | 3300 |
| 85°C | 3900 |
| 90°C | 4500 |
| 92°C 이상 | 4900 |

점 사이는 선형 보간합니다. 일반 상승은 4초 시간상수로 완화하고 2초마다 최대 300 RPM씩 적용합니다. 하강은 8초 시간상수와 2°C 차이 조건을 적용하고 마지막 속도 상승 후 20초 대기한 뒤 2초마다 최대 100 RPM씩 낮춥니다. 85°C 이상에서는 상승 지연을 생략하고 92°C 이상에서는 최대 속도를 요청합니다. 이 값들은 사용자 냉각 설정이며 Apple의 온도 한계나 모든 작업에 대한 최적값을 의미하지 않습니다. 근거와 세부 동작은 TUNING.md를 참고하세요.

## 사용

1. Stats와 Macs Fan Control을 자동 모드로 두세요. 동시에 수동 제어하지 마세요.
2. CoolCurve.app을 여세요. 처음에는 센서 읽기만 합니다.
3. 필요한 경우 대시보드의 `사용자 커브 시작…` 또는 메뉴 막대의 `온도 자동 조절 시작…`을 선택하세요.
4. macOS 관리자 인증은 팬 쓰기 권한을 얻기 위한 단계입니다. 해당 실행 동안만 보조 프로세스가 관리자 권한으로 작동하며, 로그인 항목이나 상주 시스템 서비스를 설치하지 않습니다.
5. 그만 쓸 때는 `애플 자동 제어로 복귀` 또는 `자동 복귀 후 종료`를 누르세요.

## 복귀 동작과 한계

- 센서 하나라도 읽지 못하거나 값이 유효하지 않으면 제어를 해제합니다.
- 앱의 응답이 끊기거나 앱이 종료되면 보조 프로세스가 자동 복귀를 시도합니다.
- 제어 프로세스의 비정상 종료/10초 이상 응답 정지는 별도 감시 프로세스가 감지합니다.
- 잠자기 전에 제어를 해제하고, 깨어나도 자동으로 다시 시작하지 않습니다.
- 다른 제어기가 자동 모드로 바꾼 것을 발견하면 중단합니다. 다른 앱의 수동 목표 RPM 변경은 완전히 감지하지 못하므로 동시에 제어하지 마세요.
- 팬 RPM이 목표에 크게 못 미치는 상태가 지속되면 제어를 중단합니다.
- 센서가 그럴듯한 값으로 멈추는 현상, 모든 프로세스의 동시 강제 종료, 운영체제나 SMC 정지까지 복귀를 보장할 수는 없습니다.
- 보조 프로세스가 오류를 보고하면 Stats에서 자동 모드 여부를 확인하세요.

로컬 임시 서명을 적용한 미공증 시험판입니다. 실기에서 CPU 최고 65.1→60.7°C 변화에 따라 실제 팬이 2125→1674 RPM으로 내려가는 동작을 확인했습니다. 연결 프로세스 종료 시 자동 모드와 약 1000 RPM으로 복귀하고 관리자 보조 프로세스가 종료되는 것도 확인했습니다. 장시간·고부하·잠자기/깨우기는 아직 실기 검증하지 않았습니다.

정상 복귀(종료 코드 0)에는 오류창을 표시하지 않습니다. 비정상 종료는 단계별 원인을 안내하고 Last-run.txt에 최근 실행 결과를 남깁니다.

SMC 접근 코드는 Stats 프로젝트(exelban/stats)의 MIT 라이선스 코드를 포함합니다. 원본 라이선스는 앱 Resources 및 소스 폴더에 포함했습니다.

## 0.3 대시보드

위치: `/Users/juganghyeon/CoolCurve`. 실행 앱과 `Source` 소스, `build.sh`가 같은 폴더에 있습니다.

- 대시보드: CPU·GPU 최고 온도, 실제 RPM, 제어 상태, 최근 5분 온도 그래프. 표시값은 5초마다 갱신합니다.
- 온도 커브: 현재 적용된 온도/RPM 표와 반응 지연 설명. 현재는 읽기 전용입니다.
- 활동 기록: 이번 실행의 모드 변경과 보조 프로세스 결과 최대 30개. 앱을 다시 열면 초기화됩니다.
- 창을 닫아도 메뉴 막대에서 실행됩니다. 메뉴의 대시보드 열기 또는 Dock 아이콘으로 다시 엽니다.
- 시작 시 팬 모드를 바꾸지 않습니다. 사용자 커브는 직접 시작해야 합니다. 애플 자동 상태에서 조용하고 작업 성능이 충분하다면 그대로 사용하세요.

다시 빌드하려면 이 폴더에서 `./build.sh`를 실행합니다. Apple Command Line Tools가 필요합니다.
